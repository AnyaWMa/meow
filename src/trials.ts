/**
 * Convert a string to upper case
 *
 * @function
 * @param {string} inputString - The input string
 * @returns {string} - Upper case string
 */
export const myToUpper = (inputString: string): string => {
  return inputString.toUpperCase()
};
